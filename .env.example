# Copy this file to .env and fill in real values. Never commit .env itself.

# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

SUPABASE_URL=
SUPABASE_KEY=

# Note: /api/scheduled/run is intentionally unauthenticated, matching the
# original MOCHI-Backend behavior, so your existing free-tier cron job
# (cron-job.org or similar) keeps working with no changes on its end.
